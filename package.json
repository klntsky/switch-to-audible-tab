{
  "name": "switch-to-audible-tab",
  "private": true,
  "scripts": {
    "test": "ava --verbose",
    "build": "spago bundle-app --to static/settings.js && npm run uglify",
    "uglify": "terser static/settings.js --compress --mangle --output static/settings.min.js && mv static/settings.min.js static/settings.js",
    "pack": "node scripts/pack.js",
    "pack-source": "zip -r ./switch-to-audible-tab-source.zip img src scripts manifest.json manifest.chrome.json README.md package.json package-lock.json static spago.dhall packages.dhall .gitignore LICENSE"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/klntsky/switch-to-audible-tab.git"
  },
  "devDependencies": {
    "ava": "^1.4.1",
    "purescript": "0.14.4",
    "spago": "0.20.9",
    "terser": "5.51.2"
  },
  "dependencies": {}
}
